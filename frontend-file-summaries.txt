{
  "frontend/src/components/editor/EditorTimeline.tsx": "Multi-layer video timeline component with scrubbing, clip management, and track visualization — fixed missing `X` import from lucide-react.",
  "frontend/src/pages/ExplorePage.tsx": "Explore page with Posts/Users tab switcher and debounced search; Posts tab filters the 3-column post grid by caption/author/type, Users tab searches handles via useSearchHandles and displays matching UserResultCard components (with avatar, handle, bio) that navigate to /profile/$handle.",
  "frontend/src/components/editor/StickersPanel.tsx": "New stickers/emoji panel for the editor allowing users to add, resize, rotate, and remove emoji sticker layers on the canvas.",
  "frontend/src/components/editor/FiltersPanel.tsx": "Filters and color grading panel with preset buttons and manual adjustment sliders; fixed `key` prop to use `String(key)` to avoid TypeScript symbol-type error.",
  "frontend/src/components/editor/EffectsPanel.tsx": "New motion graphics effects panel with 8 toggleable full-frame effects (confetti, glitch, letterbox, light leaks, etc.).",
  "frontend/src/components/editor/DesignPanel.tsx": "New graphics design panel for canvas background (solid colors, gradients), aspect ratio selection, shape tools, and image upload.",
  "frontend/src/components/editor/ExportPanel.tsx": "Editor export panel with video preview, title/description fields, and a download button; removed the defunct useUploadShortSportVideo hook and replaced the upload section with a note directing users to use Create Post for sharing.",
  "frontend/src/index.css": "Global stylesheet with Tailwind directives, OKLCH dark-mode-first color tokens (warm amber/rose theme for InstaVideo Social), Google Fonts import for Inter and Syne, and scrollbar-hide utility. This file is placed in `src/` so `main.tsx`'s `./index.css` import resolves correctly.",
  "frontend/index.html": "HTML entry point with the browser tab title updated to \"InstaV\".",
  "frontend/src/components/TopBar.tsx": "Sticky top navigation bar showing the InstaV brand, current page title, and a Sign In/Sign Out button; hidden on the /shortsport route; uses useRouterState for path detection and clears query cache on logout.",
  "frontend/src/pages/ProfilePage.tsx": "Authenticated profile management page with three states: login prompt for unauthenticated users, profile setup/edit form (handle, displayName, bio, profile picture upload with ExternalBlob), and profile view with edit/sign-out actions; uses createOrUpdateProfile mutation with handle uniqueness error handling.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations — includes profile CRUD (getCallerUserProfile, profileByPrincipal, profileByHandle, searchHandles, createOrUpdateProfile), post queries (getAllPosts, getPostsByUser, postsByHandle for handle-based profile pages), and mutations (createPost, likePost, recordView, addComment) with proper cache invalidation.",
  "frontend/src/components/AvatarPlaceholder.tsx": "Unified avatar component supporting both the legacy `userId`/`displayName` props and the new `name`/`profilePicture` props, with `xs`/`sm`/`md`/`lg`/`xl` sizes, gradient initials fallback, and ExternalBlob profile picture display.",
  "frontend/src/pages/EditorPage.tsx": "Rebuilt video editor page with a vertical split layout — top 50% is the video preview (with live CSS filter, sticker, and effects overlays), bottom 50% is the scrollable tools panel. All feature tabs (Upload, Trim, Filters, Effects, Stickers, Design) are now fully wired to a shared `editorState` with proper `handleStateUpdate` callbacks, and video import immediately creates an object URL and initializes editor state without any confirmation step.",
  "frontend/src/pages/ShortSportPage.tsx": "Full-screen video player page showing only video-type posts, with mute toggle, like button with optimistic updates, author/caption overlay, and navigation dots; uses `post.media?.getDirectURL()` for the video source.",
  "frontend/src/components/BottomNav.tsx": "Bottom navigation bar with five items (Home, Explore, ShortSport, Create linking to /create, Profile) using TanStack Router for active state detection; hidden on the /shortsport route.",
  "frontend/src/routeTree.ts": "TanStack Router route tree with eight routes: home, explore, editor, /profile (own profile), shortsport, create, /user/$principal (legacy), and /profile/$handle (new public handle-based profile page).",
  "frontend/src/App.tsx": "Root app component wrapping the router with `InternetIdentityProvider`, `QueryClientProvider`, `ThemeProvider` (dark default), and Sonner `Toaster`.",
  "frontend/src/components/PostCard.tsx": "Post card component that fetches the author's profile via useProfileByPrincipal to display their profile picture (AvatarPlaceholder) and handle (@handle) alongside the post; clicking the avatar/name navigates to /profile/$handle (or /user/$principal as fallback); includes like, comment, and view tracking.",
  "frontend/src/components/CommentsSheet.tsx": "Bottom sheet for viewing and adding comments on a post; fetches comments via useGetComments, displays them with author name and timestamp, and uses `userProfile?.displayName` (fixed from `.name`) as the author name when submitting new comments.",
  "frontend/src/pages/FeedPage.tsx": "Public feed page fetching all posts from backend via useGetAllPosts, displaying them newest-first as PostCard components, with loading/empty/error states and a CommentsSheet for viewing/adding comments.",
  "frontend/src/pages/CreatePostPage.tsx": "Authenticated post creation page supporting file upload (ExternalBlob) or URL input with media type selection, caption, and preview; uses `userProfile?.displayName` (fixed from `.name`) as the post author name on submission.",
  "frontend/src/pages/UserProfilePage.tsx": "Public user profile page accessible via /profile/$handle or /user/$principal routes; fetches profile via useProfileByHandle and posts via usePostsByHandle, displays avatar, handle, displayName, bio, post count, and a 3-column post grid with CommentsSheet; shows \"User not found\" when handle doesn't exist.",
  "frontend/src/components/VideoCard.tsx": "Legacy video card component for the mock-data feed, displaying post thumbnail, author info, like/comment/share counts, and bookmark; removed the CommentsSheet usage entirely since it's a legacy component using mock data types."
}