{
  "frontend/src/components/editor/EditorTimeline.tsx": "Multi-layer video timeline component with scrubbing, clip management, and track visualization — fixed missing `X` import from lucide-react.",
  "frontend/src/pages/ExplorePage.tsx": "Explore page with Posts/Users tab switcher and debounced search; replaces the removed `UserProfileSummary` backend import with a local `UserSearchResult` interface and adapts `UserResultCard` to use the `{ handle, profile }` shape returned by the updated `useSearchUsers` hook.",
  "frontend/src/components/editor/StickersPanel.tsx": "New stickers/emoji panel for the editor allowing users to add, resize, rotate, and remove emoji sticker layers on the canvas.",
  "frontend/src/components/editor/FiltersPanel.tsx": "Filters and color grading panel with preset buttons and manual adjustment sliders; fixed `key` prop to use `String(key)` to avoid TypeScript symbol-type error.",
  "frontend/src/components/editor/EffectsPanel.tsx": "New motion graphics effects panel with 8 toggleable full-frame effects (confetti, glitch, letterbox, light leaks, etc.).",
  "frontend/src/components/editor/DesignPanel.tsx": "New graphics design panel for canvas background (solid colors, gradients), aspect ratio selection, shape tools, and image upload.",
  "frontend/src/components/editor/ExportPanel.tsx": "Editor export panel with video preview, title/description fields, and a download button; removed the defunct useUploadShortSportVideo hook and replaced the upload section with a note directing users to use Create Post for sharing.",
  "frontend/src/index.css": "Global stylesheet with Tailwind directives, OKLCH dark/light color system (gold primary + coral accent), Inter/Syne fonts, scrollbar-hide utility, glassmorphism utility, story-ring gradient component, and fade-in/scale-in/pulse-glow animations.",
  "frontend/index.html": "HTML entry point with the browser tab title updated to \"Smileup\".",
  "frontend/src/components/TopBar.tsx": "Compact sticky top navigation bar (h-14) with a simple `text-xl font-bold` \"Smileup\" gradient wordmark, notification bell with unread badge, search icon, and logout button — no excessive letter-spacing or scaling.",
  "frontend/src/pages/ProfilePage.tsx": "Authenticated user's own profile page with setup/edit form, Shadows/Following stats, and a post grid; uses `open`/`onOpenChange` props on CommentsSheet instead of the removed `isOpen`/`onClose` aliases.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations; exports a local `FriendRequest` interface, adds `useGetIncomingFriendRequests` (stub returning empty array since backend has no friend-request system), and includes all profile row, glow-state, and social profile hooks alongside existing auth, post, comment, notification, message, follow, and friend hooks.",
  "frontend/src/components/AvatarPlaceholder.tsx": "Unified avatar component supporting both the legacy `userId`/`displayName` props and the new `name`/`profilePicture` props, with `xs`/`sm`/`md`/`lg`/`xl` sizes, gradient initials fallback, and ExternalBlob profile picture display.",
  "frontend/src/pages/EditorPage.tsx": "Rebuilt video editor page with a vertical split layout — top 50% is the video preview (with live CSS filter, sticker, and effects overlays), bottom 50% is the scrollable tools panel. All feature tabs (Upload, Trim, Filters, Effects, Stickers, Design) are now fully wired to a shared `editorState` with proper `handleStateUpdate` callbacks, and video import immediately creates an object URL and initializes editor state without any confirmation step.",
  "frontend/src/pages/ShortSportPage.tsx": "Full-screen ShortSport reel page with TikTok-style vertical scroll-snap; now reads the optional `postId` search parameter from the URL using `useSearch`, moves the matching post to the front of the feed array so it plays first, and scrolls to index 0 on mount when a target post is specified — falling back gracefully to the normal newest-first feed when no `postId` is present.",
  "frontend/src/components/BottomNav.tsx": "Compact bottom navigation bar (h-16) with five items (Home, Explore, ShortSport, Create, Profile) using TanStack Router for active state; hidden on /shortsport route.",
  "frontend/src/routeTree.ts": "TanStack Router route tree registering all app routes; the `/shortsport` route now includes a `validateSearch` function that accepts an optional `postId` string search parameter for deep-linking to a specific shared ShortSport.",
  "frontend/src/App.tsx": "Root app component importing `router` (not `routeTree`) from routeTree.ts, with compact loading splash, conditional LandingPage for unauthenticated users, and RouterProvider for authenticated users.",
  "frontend/src/components/PostCard.tsx": "Clean post card with compact header, square media, like/comment/share/bookmark/view actions — removed the non-existent `currentUserName` prop from CommentsSheet usage.",
  "frontend/src/components/CommentsSheet.tsx": "Bottom sheet for viewing and posting comments on a post; accepts both `open`/`onOpenChange` and `isOpen`/`onClose` prop conventions for compatibility, and passes `post?.id ?? undefined` (not null) to useGetComments.",
  "frontend/src/pages/FeedPage.tsx": "Clean home feed page with StoriesRow, TrendingProfilesSection, skeleton loading, and a sorted post list with staggered fade-in — no hero gradient, compact Version 29-style spacing.",
  "frontend/src/pages/CreatePostPage.tsx": "Authenticated post creation page supporting file upload (ExternalBlob) or URL input with media type selection, caption, and preview; uses `userProfile?.displayName` (fixed from `.name`) as the post author name on submission.",
  "frontend/src/pages/UserProfilePage.tsx": "Public user profile page with follow/unfollow, message, and friend action buttons; now includes a fire-and-forget `useEffect` that calls `actor.recordVisit` when the page mounts for a profile that is not the authenticated user's own, silently ignoring any errors.",
  "frontend/src/components/VideoCard.tsx": "Legacy video card component for the mock-data feed, displaying post thumbnail, author info, like/comment/share counts, and bookmark; removed the CommentsSheet usage entirely since it's a legacy component using mock data types.",
  "frontend/src/pages/MessageThreadPage.tsx": "Thin wrapper page component that reads the principalId route param and renders MessagesPage with it as the initialPrincipal prop.",
  "frontend/src/components/Layout.tsx": "Minimal root layout wrapping TopBar, router Outlet (with pt-14 pb-16 for nav bars), and BottomNav in a full-height flex column.",
  "frontend/src/pages/MessagesPage.tsx": "Full messaging page with Inbox/Social/Friend Zone tabs; removes the non-existent `FriendRequest` import from the backend module and replaces it with a locally-defined interface, while keeping all existing conversation, thread, and friend-request UI intact.",
  "frontend/src/pages/LandingPage.tsx": "Professional landing/splash page for unauthenticated users featuring a large text-only \"Smileup\" gradient wordmark, tagline, gradient \"Get Started\" login button, feature highlights grid, and branded footer.",
  "frontend/tailwind.config.js": "Tailwind config with OKLCH color tokens, Inter/Syne font families, custom border-radius scale, card/gold-glow/coral-glow shadows, and fade-in/scale-in/pulse-glow animation keyframes.",
  "frontend/src/lib/mockData.ts": "Mock data module with User, Post, Story, Comment, Conversation interfaces; adds MOCK_STORIES array with 7 story entries referencing MOCK_USERS, and expands MOCK_USERS to 7 creators.",
  "frontend/src/components/StoriesRow.tsx": "Horizontally-scrollable stories row that fetches profile row users from the backend, shows their avatars with a glow ring when they have new posts, and navigates to the user's latest post on click if glowing; restores proper `YourStoryPopup` usage with local `showYourStoryPopup` state and all required props.",
  "frontend/src/components/StoryViewerModal.tsx": "New fullscreen story viewer modal with gradient background, progress bar, author avatar/name header, close button, and tap-outside-to-close behavior.",
  "frontend/src/components/TrendingProfilesSection.tsx": "Live-data Trending Creators section that fetches the top 10 creators by shadow count using `useGetTopCreators`, displaying rank badge (with gold/silver/bronze medal emojis for top 3), profile avatar via `AvatarPlaceholder`, username, and formatted shadow count; includes loading skeletons and an empty state.",
  "frontend/src/components/ShareModal.tsx": "New modal component for sharing a post to friends; displays the caller's friends list with multi-select checkboxes, sends a message with the postId to each selected friend via `useSendMessage`, and shows a success toast on completion.",
  "frontend/src/components/YourStoryPopup.tsx": "Simple dropdown popup anchored below the \"Your Story\" button with \"View My Posts\" and \"Create New Post\" options, click-outside detection, and clean card styling."
}